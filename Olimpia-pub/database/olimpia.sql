-- Olimpia: solo crea la base de datos.
-- El esquema de tablas se gestiona con migraciones Laravel:
--   php artisan migrate
--   php artisan db:seed

CREATE DATABASE IF NOT EXISTS olimpia
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
